<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ict.mapper.BoardMapper">

<select id="getList" resultType="org.ict.domain.ReplyVO">
<![CDATA[
SELECT * FROM reply_tbl WHERE bno=#{bno}
OREDER BY DESC
]]>
</select>

<insert id="create">
<![CDATA[
INSERT INTO reply_tbl(rno, bno, reply, replyer, replydate) VALUES (reply_num.NEXTVAL, #{bno} #{reply}, #{replyer})
]]>
</insert>

<update id="update">
<![CDATA[
UPDATE board_tbl SET reply = #{reply}, updateDate = SYSDATE
WHERE rno = #{rno}
]]>
</update>

<delete id="delete">
<![CDATA[
DELETE FROM board_tbl WHERE rno=#{rno}
]]>
</delete>

</mapper>